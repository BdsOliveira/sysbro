/* function addOneHourToDateTime(sells = {}) {
    for (let i = 0; i < sells.length; i++) {
        let convertedDate = new Date(sells[i].date);
        convertedDate.setHours(convertedDate.getHours() + 1)
        sells[i].date = convertedDate
    }
    return sells
}
module.exports = addOneHourToDateTime; */